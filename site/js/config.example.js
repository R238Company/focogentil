// Modelo de configuração. Copie este arquivo para "config.js" (ou rode
// infra/deploy.ps1, que gera "config.js" automaticamente com a URL real da
// sua API depois do deploy). "config.js" nunca deve ir para o repositório
// público, pois contém a URL ativa de escrita da sua própria implantação.
window.FOCOGENTIL_CONFIG = {
  API_URL: "__API_URL__"
};
